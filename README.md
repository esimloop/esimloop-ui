# SEO Platform Design System

A starter kit for SEO / analytics products: **two design systems** — a data-dense
application layer and an editorial marketing layer — with **swappable themes** and an
Astro scaffold.

Reconstructed from the OpenSEO open-source implementation (MIT), rebuilt brand-neutral.
No logo, name, marketing copy or illustration is carried over — only the system and UX
language. Placeholders are `Product`, `Workspace`, `Project`.

```bash
node bin/init.mjs ../my-project --name my-project --theme blank
cd ../my-project && npm install && npm run dev
```

## The one idea

**Skeleton and skin are separate files, and the split is enforced.**

| | Skeleton | Skin |
| --- | --- | --- |
| Files | `css/system.css`, `css/marketing.css` | `themes/*.css` |
| Contains | component rules, layout, states | token values only |
| Per project | never edit | **this is what you edit** |

`npm run check` fails the build if the skeleton hard-codes a value, or if any theme
drifts from the others. That guard is what makes themes genuinely interchangeable —
which is what stops every project you build from looking identical.

## Two layers

| | Application (`ds-`) | Marketing (`mk-`) |
| --- | --- | --- |
| Ground | neutral grey | four warm bands |
| Accent | used throughout | used **once per page** |
| Largest type | 24px | 72px, fluid |
| Display weight | 600 | 500 |
| Button | 40px, weight 600 | 44px, weight 500, never a pill |
| Shadow | floating layers only | floating layers **and** the nav |
| Themes | light + dark | **light only** |

They share a typeface and nothing else. Don't mix their classes on one page.

## Layout

| Path | What it is |
| --- | --- |
| `themes/app-blank.css` | **Start here.** Neutral, AA-clean app theme. |
| `themes/app-openseo.css` | The reconstructed values. |
| `themes/marketing-blank.css` | Neutral marketing theme. |
| `themes/marketing-openseo.css` | The reconstructed values (warm, orange accent). |
| `css/system.css` | App components. References tokens, never declares them. |
| `css/marketing.css` | Marketing components. Same rule. |
| `css/index.css` | Loads both skeletons. |
| `astro/app/` | 15 Astro components + the lucide sprite (33 icons). |
| `astro/marketing/` | 8 Astro components. |
| `bin/init.mjs` | Scaffolds a new Astro project. |
| `scripts/check-tokens.mjs` | The contract guard. |
| `docs/design-system.html` | The full specification — 12 sections, 4 proof screens. |
| `example/` | A working Astro app, generated by `init`. |

## Commands

```bash
npm run check            # token contract guard
npm run example          # regenerate example/ from the templates
npm run example:build    # prove it compiles
npm run verify           # all of the above
```

## Re-skinning a project

Open `src/design-system/themes/app-blank.css` and change four things:

1. `--accent` — your brand colour. Must reach **4.5:1** against `--surface-card`.
2. `--content` — your ink. Every text tier is this at an opacity, so it drives the
   whole grey scale.
3. `--surface-app` / `--surface-card` / `--surface-sunken` — the ramp.
4. `--font-sans`.

Everything else derives. For the marketing layer, keep the *structure* even when you
change the values: four close-together grounds separated by hairlines, and one accent
used once per page.

## What the blank theme fixes

The reconstructed theme shares its four feedback hues between light and dark, tuned
against the dark ground. On white, `warning` (1.9:1) and `info` (2.6:1) fail AA as text.
`themes/app-blank.css` gives light and dark their own sets, all AA as text — measured
ratios are in the comments. If you use `app-openseo.css`, follow the constraint in
section 09 of the spec: semantic hue as fill, border or dot, label in the foreground
colour.

## Deliberate deviations

- **Audit severity is three tiers, not five** (`critical` / `warning` / `info`, plus a
  passed state). The palette has no fifth semantic hue. A five-tier mapping is in
  section 04 of the spec.
- **Mobile tables scroll horizontally.** Not reflowed into cards, no columns dropped —
  these tables exist to be compared across, and card reflow destroys the column axis.
  Mark the identifier column `data-col="id"` so it keeps a readable width.
- **Toast, sidebar collapse, nested navigation and breadcrumbs** are extensions, absent
  from the original. Labelled in *Open questions* at the end of the spec.

## Verified

Node 24.20.0 · Astro 7.3.2. `example/` installs and builds clean; both pages were
checked at 1280px, mobile (375px), and in dark mode.

## Licence

OpenSEO is MIT (Copyright 2026 Ben Senescu) — commercial use permitted; retain the
copyright notice if you reuse its source. The CSS and components here are original
implementations, so that obligation does not attach to them.
